import { useEffect, useMemo, useState } from "react"

// Acá elaboro las validaciones del login

export function useLogin() {
    const [username, setUsername] = useState(null) // <- Estado que permite almacenar la información del usuario
    const [password, setPassword] = useState(null) // <- Estado que permite almacenar la información de la contraseña


}